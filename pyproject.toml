[project]
name = "hallucistation"
version = "0.1.0"
description = "Oráculo Alucinado - API que gera ideias alucinadas usando heurísticas"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "pydantic>=2.0.0"
]
